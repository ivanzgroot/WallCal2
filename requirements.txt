flask>=3.0.0
caldav>=1.3.0
icalendar>=5.0.0
python-dateutil>=2.8.0
cryptography>=41.0.0
pyserial>=3.5

# QR encoding for the companion code (§5.5). Pure Python, zero runtime
# dependencies, ~100 kB. Justified over hand-rolling it: correct QR output
# needs Reed-Solomon error correction, version selection and mask evaluation,
# which is a lot of subtle code for something that has to just work.
segno>=1.6.0

